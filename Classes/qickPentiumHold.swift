 
 
 
 
 
 

import UIKit

class qickPentiumHold: lrnRespondCall {
    var chrAmountCard:lrnRespondSend?
    lazy var cntnFlagInput = {
        let cptreBothEasel = UIImageView().dscssPriceDeselected(certnAgainProxy("238"))
        return cptreBothEasel
    }()
    lazy var cntrlWordShell = {
       let keywrdTrueBegin = UILabel()
            .cnsleNameCare(.center)
            .referenceCellPunch(.SrceShiftDecoder)
            .ccrcyOnceInvent(.systemFont(ofSize: 14, weight: .medium))
            .ssctedChartScan(true)
        return keywrdTrueBegin
    }()
    lazy var clssWareCause = {
        let keywrdTrueBegin = UILabel()
            .cnsleNameCare(.center)
            .referenceCellPunch(.SrceStillKind)
            .ccrcyOnceInvent(.systemFont(ofSize: 12, weight: .bold))
         return keywrdTrueBegin
     }()
    
    lazy var prctceActivityTeam = {
        let keywrdTrueBegin = UILabel()
            .cnsleNameCare(.center)
            .referenceCellPunch(.SrceShiftDecoder)
            .ccrcyOnceInvent(.systemFont(ofSize: 11, weight: .regular))
         return keywrdTrueBegin
     }()
    
    lazy var extendedRuleAvoid = {
        let keywrdTrueBegin = UILabel()
            .cnsleNameCare(.center)
            .referenceCellPunch(.SrceStillKind)
            .ccrcyOnceInvent(.systemFont(ofSize: 26, weight: .bold))
         return keywrdTrueBegin
     }()
    
    override func keywrdNameSince() {
        addSubview(cntnFlagInput)
        addSubview(cntrlWordShell)
        addSubview(clssWareCause)
        addSubview(extendedRuleAvoid)
        addSubview(prctceActivityTeam)
        self.layer.backgroundColor = UIColor.lbrryRemainPath(trnsfrmUnitUser: 0x1B1C20).cgColor
        self.layer.cornerRadius = 6
    }
    
    override func updateConstraints() {
        super.updateConstraints()
        cntnFlagInput.snp.remakeConstraints { make in
            make.left.top.equalToSuperview()
            make.height.equalTo(24)
            make.width.equalTo(72)
        }
        
        cntrlWordShell.snp.remakeConstraints { make in
            make.center.equalTo(cntnFlagInput.snp.center)
            make.width.lessThanOrEqualTo(62)
        }
        
        clssWareCause.snp.remakeConstraints { make in
            make.centerX.equalToSuperview()
            make.width.equalToSuperview()
            make.top.equalTo(cntnFlagInput.snp.bottom).offset(14)
        }
        
        extendedRuleAvoid.snp.remakeConstraints { make in
            make.centerX.equalToSuperview()
            make.height.greaterThanOrEqualTo(33)
            make.width.equalToSuperview()
            make.top.equalTo(clssWareCause.snp.bottom).offset(14)
        }
        
        prctceActivityTeam.snp.remakeConstraints { make in
            make.centerX.width.equalToSuperview()
            make.top.equalTo(extendedRuleAvoid.snp.bottom).offset(11)
            make.bottom.equalTo(-13)
        }
    }
    
    
    
    override func layoutSubviews() {
        super.layoutSubviews()
        guard let chrAmountCard = chrAmountCard else {return}
        if chrAmountCard.ptmzeWrongAtomically {
            self.sterskWorkTitle(selectnBoldUpdate: [UIColor.lbrryRemainPath(trnsfrmUnitUser: 0xEDC391).cgColor,UIColor.lbrryRemainPath(trnsfrmUnitUser: 0xFDDDB7).cgColor])
        } else {
            self.emplyeUpdateRemark()
        }
    }
}
