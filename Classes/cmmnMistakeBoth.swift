 
 
 
 
 
 

import Foundation


extension qickPentiumHold {
    
    func derelctAlphaCombine(){
        if let chrAmountCard = chrAmountCard {
            clssWareCause.textColor = chrAmountCard.ptmzeWrongAtomically ? .SrceStillKind : .SrceShiftDecoder
            extendedRuleAvoid.textColor = chrAmountCard.ptmzeWrongAtomically ? .SrceStillKind : .SrceShiftDecoder
            prctceActivityTeam.textColor = chrAmountCard.ptmzeWrongAtomically ? .SrceStillKind : .RgnUniqueData
            cntrlWordShell.text = chrAmountCard.temFooterLoss
            clssWareCause.text = chrAmountCard.replceLogicWait
            extendedRuleAvoid.text = chrAmountCard.sggestPastPrice
            
            if chrAmountCard.nptStepPause == EncryptRequiredTiny || chrAmountCard.nptStepPause == NptHalfBorder || chrAmountCard.dscssHoldMatch {
                let attributedText = NSAttributedString(string: chrAmountCard.expressRemakeNegate ?? "", attributes: [NSAttributedString.Key.strikethroughStyle: NSUnderlineStyle.single.rawValue])
                prctceActivityTeam.attributedText = attributedText
            } else {
                prctceActivityTeam.text = chrAmountCard.expressRemakeNegate
            }
            
            cntnFlagInput.isHidden = (chrAmountCard.temFooterLoss == nil)
            cntrlWordShell.isHidden = (chrAmountCard.temFooterLoss == nil)
            layoutSubviews()
        }
    }
}
