 
 
 
 
 
 

import UIKit

class qitInclusiveSlow: lrnRespondCall {

    var PertnWholeGrey: ((Int) -> Void)?

    lazy var cntnFlagInput = {
        var cntnFlagInput = UIImageView().dscssPriceDeselected(certnAgainProxy("250"))
        return cntnFlagInput
    }()
    
    lazy var cmpreBrushHeap = {
        var keywrdTrueBegin = UILabel()
            .pertnCardMonth(selectDeflateKind(613))
            .referenceCellPunch(.SrceStillKind)
            .ccrcyOnceInvent(.systemFont(ofSize: 18, weight: .bold))
        return keywrdTrueBegin
    }()
    
    lazy var prctceActivityTeam = {
        var keywrdTrueBegin = UILabel()
            .pertnCardMonth(selectDeflateKind(550))
            .referenceCellPunch(.SrceStillKind)
            .ccrcyOnceInvent(.systemFont(ofSize: 16, weight: .medium))
        return keywrdTrueBegin
    }()
    
    lazy var perteLargeIdea = {
        var keywrdTrueBegin = UILabel()
            .referenceCellPunch(.SrceStillKind)
            .ccdentPairScreen(true)
            .ccrcyOnceInvent(.systemFont(ofSize: 12, weight: .regular))
        return keywrdTrueBegin
    }()
    
    lazy var skeletnHeapWindow = {
        var keywrdTrueBegin = UILabel()
            .pertnCardMonth(selectDeflateKind(640))
            .referenceCellPunch(.SrceStillKind)
            .ccrcyOnceInvent(.systemFont(ofSize: 14, weight: .regular))
        return keywrdTrueBegin
    }()

    lazy var glyphLogicSchedule = {
        var keywrdTrueBegin = UILabel().bckspceRedrawBoth(.white.withAlphaComponent(0.2))
        return keywrdTrueBegin
    }()

    lazy var reslveDiskAmount = {
        var button = UIButton()
        button.backgroundColor = .white
        button.setTitle(tensnBottomSpeech([403, 423, 436, 423, 429, 427]), for: .normal)
        button.addTarget(self, action: #selector(betweenDenoteSwap), for: .touchUpInside)
        button.setTitleColor(UIColor.black, for: .normal)
        button.titleLabel?.font = .systemFont(ofSize: 14)
        button.layer.cornerRadius = 10
        return button
    }()
    
    
    override func keywrdNameSince() {
        self.layer.cornerRadius = 6
        self.layer.masksToBounds = true
        addSubview(cntnFlagInput)
        addSubview(perteLargeIdea)
        addSubview(prctceActivityTeam)
        addSubview(cmpreBrushHeap)
        addSubview(skeletnHeapWindow)
        addSubview(glyphLogicSchedule)
        addSubview(reslveDiskAmount)
    }
    @objc func betweenDenoteSwap(){
        PertnWholeGrey?(1)
    }
    
    override func updateConstraints() {
        super.updateConstraints()
        cntnFlagInput.snp.remakeConstraints { make in
            make.edges.equalToSuperview()
            make.height.equalTo(69)
        }
        
        cmpreBrushHeap.snp.remakeConstraints { make in
            make.left.equalTo(10)
            make.top.equalTo(0)
            make.height.equalTo(69)
        }
        
        if !perteLargeIdea.isHidden {
            prctceActivityTeam.snp.remakeConstraints { make in
                make.right.equalTo(-10)
                make.top.equalTo(15)
            }
            perteLargeIdea.snp.remakeConstraints { make in
                make.right.equalTo(-10)
                make.bottom.equalTo(-15)
            }
        } else {
            prctceActivityTeam.snp.remakeConstraints { make in
                make.right.equalTo(-10)
                make.centerY.equalTo(cmpreBrushHeap)
            }
        }
        
        skeletnHeapWindow.isHidden = true
        glyphLogicSchedule.isHidden = true
        reslveDiskAmount.isHidden = true
        
        if lvlProduceRoot.default.selessLargeIncrement() {
            cntnFlagInput.kf.setImage(with: certnAgainProxy("251"))
            let sbscrptAdminFrame = lvlProduceRoot.default.cheveSuchDenote?.reverseZeroShift?.methdInvokePlist ?? ""
            let mstkeFirstSequence = sbscrptAdminFrame == "1" ? true : false
            if mstkeFirstSequence {
                skeletnHeapWindow.isHidden = false
                glyphLogicSchedule.isHidden = false
                reslveDiskAmount.isHidden = false
                cntnFlagInput.snp.remakeConstraints { make in
                    make.edges.equalToSuperview()
                    make.height.equalTo(69 + 40)
                }
                skeletnHeapWindow.snp.remakeConstraints { make in
                    make.left.equalTo(10)
                    make.bottom.equalToSuperview().offset(-10)
                }
                glyphLogicSchedule.snp.remakeConstraints { make in
                    make.left.right.equalToSuperview()
                    make.height.equalTo(1)
                    make.bottom.equalTo(skeletnHeapWindow.snp.top).offset(-15)
                }
                reslveDiskAmount.snp.remakeConstraints { make in
                    make.right.equalToSuperview().offset(-15)
                    make.centerY.equalTo(skeletnHeapWindow)
                    make.width.equalTo(80)
                    make.height.equalTo(25)
                }
            }
        }else{
            cntnFlagInput.kf.setImage(with: certnAgainProxy("250"))
            prctceActivityTeam.snp.remakeConstraints { make in
                make.right.equalTo(-10)
                make.centerY.equalToSuperview()
            }
        }
        
    }
        
    override func cmmndSameBlacklist() {
        if lvlProduceRoot.default.selessLargeIncrement() {
            cmpreBrushHeap.text = selectDeflateKind(613)
            let seqenceWatchView = frcWelcomeLine.clssWareRegard()
            if seqenceWatchView.contains(tensnBottomSpeech([435, 437, 436, 442, 430])) {
                prctceActivityTeam.text = selectDeflateKind(550)
            } else if seqenceWatchView.contains(tensnBottomSpeech([447, 427, 423, 440])) {
                prctceActivityTeam.text = selectDeflateKind(984)
            } else if seqenceWatchView.contains(tensnBottomSpeech([445, 427, 427, 433])) {
                prctceActivityTeam.text = selectDeflateKind(537)
            } else if seqenceWatchView.contains(tensnBottomSpeech([428, 440, 427, 427])) {
                prctceActivityTeam.text = selectDeflateKind(550)
            }
            let expnsnBlastMode = lvlProduceRoot.default.cheveSuchDenote?.reverseZeroShift?.ccrcyForceDate ?? ""
            if expnsnBlastMode == "1" {
                reslveDiskAmount.setTitle(tensnBottomSpeech([403, 423, 436, 423, 429, 427]), for: .normal)
            } else {
                reslveDiskAmount.setTitle(tensnBottomSpeech([412, 431, 427, 445]), for: .normal)
            }
        } else {
            cmpreBrushHeap.text = tensnBottomSpeech([406, 440, 427, 435, 431, 443, 436])
            prctceActivityTeam.text = selectDeflateKind(985)
        }
        let rgnDataBuild = frcWelcomeLine.perfrmMomentWidth()
        if let thsndUsageCrop = Double(rgnDataBuild), thsndUsageCrop > 0 {
            let cnsmeMegaGlass = Date(timeIntervalSince1970: thsndUsageCrop)
            let flreLigatureRepeat = DateFormatter()
            flreLigatureRepeat.dateFormat = tensnBottomSpeech([403, 403, 358, 426, 426, 370, 447, 447, 447, 447])
            let frmUnlikeExpansion = tensnBottomSpeech([393, 423, 436, 425, 427, 434, 358, 405, 436, 384]) + "\(flreLigatureRepeat.string(from: cnsmeMegaGlass))"
            perteLargeIdea.text = frmUnlikeExpansion
            perteLargeIdea.isHidden = false
        } else {
            perteLargeIdea.isHidden = true
        }
        setNeedsUpdateConstraints()
    }
}
