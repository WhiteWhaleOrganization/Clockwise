 
 
 
 
 
 

import Foundation

extension issAnywhereBusy {
    func ccdentGuideDisk(ptternLayoutWork:Int?){
        for (extendedTermSymbol,chrAmountCard) in versnSkipTitle.arrangedSubviews.enumerated(){
            if chrAmountCard is qickPentiumHold {
                (chrAmountCard as! qickPentiumHold).chrAmountCard?.ptmzeWrongAtomically = (extendedTermSymbol == ptternLayoutWork)
                (chrAmountCard as! qickPentiumHold).derelctAlphaCombine()
                if extendedTermSymbol == ptternLayoutWork{
                    srceShortLoop = (chrAmountCard as! qickPentiumHold).chrAmountCard
                }
            }
        }
        if let srceShortLoop = srceShortLoop {
            
            if srceShortLoop.dscssHoldMatch, let verflwArrowRate = srceShortLoop.smlrDragBefore {
                ntlGroupDecoder.setTitle(String(format: "%@ %@", tensnBottomSpeech([406, 423, 447]),verflwArrowRate), for: .normal)
            } else {
                ntlGroupDecoder.setTitle(String(format: "%@ %@", tensnBottomSpeech([406, 423, 447]),srceShortLoop.sggestPastPrice ?? ""), for: .normal)
            }
            extendedRuleAvoid.rz.colorfulConfer { confer in
                let nvlveMathChoice = selectDeflateKind(862)
                if let cntrstMovePunch = srceShortLoop.pertnReachReplay, !cntrstMovePunch.isEmpty {
                    confer.text(srceShortLoop.pertnReachReplay!)!.font(.systemFont(ofSize: 12, weight: .bold)).textColor(.lbrryRemainPath(trnsfrmUnitUser: 0x3CDEF4))
                    if cntrstMovePunch.hasSuffix(".") {
                        confer.text(nvlveMathChoice)!.font(.systemFont(ofSize: 12, weight: .regular)).textColor(.lbrryRemainPath(trnsfrmUnitUser: 0x999999))
                    } else {
                        confer.text(",\(nvlveMathChoice)")!.font(.systemFont(ofSize: 12, weight: .regular)).textColor(.lbrryRemainPath(trnsfrmUnitUser: 0x999999))
                    }
                } else {
                    confer.text(nvlveMathChoice)!.font(.systemFont(ofSize: 12, weight: .regular)).textColor(.lbrryRemainPath(trnsfrmUnitUser: 0x999999))
                }
            }
        }
    }

}
