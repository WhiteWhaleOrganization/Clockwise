 
 
 
 
 
 

import Foundation

extension whitInquiryStay {
    func bndryCircleVice(sbscrptWeaklySymbol:srchControlThen,keywrdTrueBegin:String){
        sbscrptWeaklySymbol.keywrdTrueBegin.text = keywrdTrueBegin
        sbscrptWeaklySymbol.keywrdTrueBegin.textColor = .lbrryRemainPath(trnsfrmUnitUser: 0xEAE9EE)
        sbscrptWeaklySymbol.keywrdTrueBegin.font = .systemFont(ofSize: 12, weight: .bold)
        sbscrptWeaklySymbol.cnImplementUsage.kf.setImage(with: certnAgainProxy("212"))
        sbscrptWeaklySymbol.verllWrongDrag = CGSizeMake(15, 15)
        sbscrptWeaklySymbol.preserveInvokeRegard = .init(top: 0, left: 10, bottom: 0, right: 0)
        sbscrptWeaklySymbol.smmryShortSkip = .init(top: 16, left: 10, bottom: 16, right: 0)
    }

}
